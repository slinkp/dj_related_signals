from setuptools import setup, find_packages

setup(
    name="dj_related_signals",
    url="https://github.com/slinkp/dj_related_signals",
    version="0.1",
    packages=find_packages(),
    description="",
    install_requires=[
        "Django>=1.8",
    ],
    zip_safe=False,
)
